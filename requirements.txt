# Основы машинного обучения — магистратура лингвистики
#
#     pip install -r requirements.txt
#
# Все версии ниже ПРОВЕРЕНЫ: на них прогнаны все четыре лабораторные
# работы, каждая выполняется сверху вниз без ошибок.
#
# Проверено на: Python 3.12.10, Windows 11, AMD64.

# --- обязательное: Л.р. № 1, 2, 3 ------------------------------------
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.7.2
matplotlib==3.8.4
scipy==1.13.0
nltk==3.9.2

# --- необязательное: раздел 9 Л.р. № 3, эталонные метрики -------------
sacrebleu==2.6.0

# --- Л.р. № 4: демонстрация генерации текста --------------------------
#
# ВНИМАНИЕ по версиям TensorFlow.
#
# Для Python 3.12 доступны только TensorFlow 2.16 и новее, а все они
# работают на Keras 3. TensorFlow 2.15 и старше (Keras 2) собран лишь
# для Python 3.11 и ниже — зафиксировать его на 3.12 невозможно.
#
# Исправленный ноутбук Л.р. № 4 работает на Keras 3: проверен полностью,
# все 43 кодовые ячейки. Три ячейки, несовместимые с Keras 3
# (tf.saved_model.save для подкласса Model, обращение к незагруженной
# модели, раздел Advanced Training), из ноутбука удалены.
#
tensorflow==2.20.0        # проверено; keras 3.12.0 ставится автоматически

# --- необязательное: раздел 10 Л.р. № 3 -------------------------------
#
# Нужно ТОЛЬКО чтобы воспроизвести машинный перевод самостоятельно.
# Сам перевод уже лежит в корпусе, а эмбеддинги — в data/*.npy,
# поэтому для выполнения работ эти пакеты НЕ требуются.
#
# transformers==4.57.1
# torch==2.9.0                  # CPU-сборка; на машине с CUDA ставьте
#                               # сборку под свою версию CUDA
# sentencepiece==0.2.2
# sacremoses==0.2.0
#
# Три предостережения, проверенные на практике:
#
# 1. ПОРЯДОК ИМПОРТА НА WINDOWS. torch нужно импортировать ДО numpy,
#    pandas, sklearn и scipy. При обратном порядке:
#        OSError: [WinError 1114] A dynamic link library (DLL)
#        initialization routine failed
#    Поэтому прогон перевода выносите в отдельный файл, а не в ячейку
#    ноутбука, где pandas уже импортирован.
#
# 2. TORCHVISION ДОЛЖЕН СООТВЕТСТВОВАТЬ TORCH. transformers импортирует
#    torchvision; при несовпадении версий получите
#        RuntimeError: operator torchvision::nms does not exist
#    Соответствие: torch 2.9 ↔ torchvision 0.24, torch 2.11 ↔ 0.26.
#
# 3. НЕ СТАВЬТЕ CPU-СБОРКУ ПОВЕРХ CUDA-СБОРКИ. Команда вида
#    `pip install torch==2.9.0` на машине с CUDA-сборкой молча заменит
#    её на CPU-версию и рассогласует torchvision и torchaudio.
#    Ставьте с индекса своей версии CUDA, например:
#        pip install --index-url https://download.pytorch.org/whl/cu128 \
#            torch==2.11.0 torchvision==0.26.0
